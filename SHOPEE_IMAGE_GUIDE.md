# 🖼️ คู่มือดึงรูปภาพจาก Shopee

เนื่องจาก Shopee ไม่มี API สาธารณะสำหรับ affiliate ให้เราใช้เครื่องมือเหล่านี้เพื่อดึงรูปภาพสินค้า

---

## 📋 วิธีการทั้งหมด

มี **4 วิธี** ในการดึงรูปภาพจาก Shopee:

1. **🤖 Playwright Automation** (แนะนำ - อัตโนมัติ 100%)
2. **🔖 Bookmarklet + Manual** (ง่าย ไม่ต้องติดตั้งอะไร)
3. **⌨️ Interactive Script** (กึ่งอัตโนมัติ)
4. **📝 CSV with URLs** (ถ้ามีลิงก์รูปแล้ว)

---

## 🤖 วิธีที่ 1: Playwright Automation (แนะนำ - อัตโนมัติ 100%)

### ✅ ข้อดี
- **อัตโนมัติเต็มรูปแบบ** - แค่รัน script เดียวเสร็จทุกอย่าง
- **ดึงรูปได้หลายรูป** - ดึงรูปทั้งหมดจากหน้าสินค้า
- **รองรับวิดีโอ** - ดึงวิดีโอสินค้าได้ด้วย
- **ทำงานพื้นหลัง** - ใช้ headless mode ทำงานเร็วกว่า

### 📦 ติดตั้ง (ครั้งเดียว)

```bash
# ติดตั้ง Playwright
npm install --save-dev playwright

# ติดตั้ง browser (Chromium)
npx playwright install chromium
```

### 🚀 วิธีใช้งาน

#### แบบพื้นฐาน (แสดง browser)
```bash
node scripts/fetch-shopee-with-playwright.js input.csv output.csv
```

#### แบบ Headless (เร็วกว่า ไม่แสดงหน้าต่าง)
```bash
node scripts/fetch-shopee-with-playwright.js input.csv output.csv --headless
```

#### ทดสอบก่อน (สินค้า 3 ตัวแรก)
```bash
node scripts/fetch-shopee-with-playwright.js input.csv output.csv --limit=3
```

#### ดึงแค่ลิงก์ (ไม่ดาวน์โหลดรูป)
```bash
node scripts/fetch-shopee-with-playwright.js input.csv output.csv --skip-download
```

#### ตั้งค่าเวลาหน่วง (ป้องกัน rate limit)
```bash
node scripts/fetch-shopee-with-playwright.js input.csv output.csv --delay=3000
```

### 📄 ตัวอย่าง CSV Input

```csv
รหัสสินค้า,ชื่อสินค้า,ลิงก์สินค้า
2594805678,น้ำยาทำความสะอาดรองเท้า,https://shopee.co.th/product/28794063/2594805678
54103031406,Adidas Jacket,https://shopee.co.th/product/1670209903/54103031406
```

### 📊 ผลลัพธ์

Script จะ:
1. เปิด browser อัตโนมัติ
2. เข้าหน้าสินค้าแต่ละตัว
3. รอให้รูปโหลดเสร็จ
4. ดึง URL รูปทั้งหมด
5. ดาวน์โหลดรูปมาเก็บใน `/public/products/{productId}/`
6. สร้าง CSV ใหม่พร้อมข้อมูล:
   - `imageUrls` - ลิงก์รูปต้นฉบับ (คั่นด้วย |)
   - `downloadedImages` - path รูปที่ดาวน์โหลด (คั่นด้วย |)
   - `imagesCount` - จำนวนรูป
   - `videosCount` - จำนวนวิดีโอ
   - `mediaStatus` - สถานะ (success/error)

### 💡 Tips

- ใช้ `--headless` เพื่อทำงานเร็วขึ้น
- ใช้ `--limit=N` เพื่อทดสอบก่อนรันเต็ม
- ใช้ `--delay=3000` (3 วินาที) ถ้า Shopee บล็อก request
- สามารถรันหลายครั้งได้ script จะข้ามสินค้าที่มีรูปแล้ว

---

## 🔖 วิธีที่ 2: Bookmarklet + Manual

### ขั้นตอนที่ 1: เปิดเครื่องมือ Extractor

```bash
# เปิด dev server
npm run dev
```

จากนั้นเปิด browser ไปที่:
```
http://localhost:3000/shopee-image-extractor.html
```

### ขั้นตอนที่ 2: ติดตั้ง Bookmarklet

1. แสดง **Bookmark Bar** ใน browser (กด `Cmd+Shift+B` บน Mac หรือ `Ctrl+Shift+B` บน Windows)
2. **ลากปุ่ม "📸 Extract Shopee Images"** ไปวางบน Bookmark Bar

### ขั้นตอนที่ 3: ดึงรูปจาก Shopee

1. เปิดหน้าสินค้า Shopee ที่ต้องการ เช่น:
   ```
   https://shopee.co.th/product/28794063/2594805678
   ```

2. รอให้หน้าเว็บโหลดเสร็จสมบูรณ์ (รูปภาพทั้งหมดแสดงแล้ว)

3. **กดที่ bookmark** "📸 Extract Shopee Images" ที่สร้างไว้

4. จะมี popup แสดงว่า "คัดลอก X รูปภาพแล้ว!"

5. กลับไปที่หน้า `shopee-image-extractor.html`

6. **วาง (Paste)** ลงใน textarea (กด `Cmd+V` หรือ `Ctrl+V`)

7. กดปุ่ม **"🔍 ดึงรูปภาพ"**

8. จะเห็นรูปภาพทั้งหมด พร้อมลิงก์

### ขั้นตอนที่ 4: อัพเดท CSV

คัดลอกลิงก์รูปที่ได้ แล้วเพิ่มเข้าไปใน CSV ในคอลัมน์ `imageUrls`:

```csv
รหัสสินค้า,ชื่อสินค้า,imageUrls
2594805678,น้ำยาทำความสะอาดรองเท้า,https://down-th.img.susercontent.com/file/xxx|https://down-th.img.susercontent.com/file/yyy
```

**หมายเหตุ:** ใช้ `|` (pipe) คั่นระหว่างลิงก์รูป

### ขั้นตอนที่ 5: ดาวน์โหลดรูปภาพ

```bash
node scripts/download-images-from-urls.js your-file.csv output.csv
```

---

## ⌨️ วิธีที่ 2: Interactive Script (กึ่งอัตโนมัติ)

สำหรับคนที่ชอบใช้ Terminal:

```bash
node scripts/download-images-from-urls.js input.csv output.csv --interactive
```

Script จะถามลิงก์รูปสำหรับแต่ละสินค้า:

```
==============================================================
📦 สินค้า: น้ำยาทำความสะอาดรองเท้า ROCKER
🆔 ID: 2594805678
==============================================================

ใส่ลิงก์รูปภาพ (ทีละบรรทัด, เสร็จแล้วพิมพ์ "done"):
หรือพิมพ์ "skip" เพื่อข้าม

> https://down-th.img.susercontent.com/file/xxx
  ✓ เพิ่มแล้ว (1)
> https://down-th.img.susercontent.com/file/yyy
  ✓ เพิ่มแล้ว (2)
> done

  ⬇️  [1/2] Downloading...
  ✅ Saved: image-1.jpg
  ⬇️  [2/2] Downloading...
  ✅ Saved: image-2.jpg
```

**วิธีหาลิงก์รูป:**
1. เปิดหน้าสินค้า Shopee
2. คลิกขวาที่รูป → "Open Image in New Tab" หรือ "Copy Image Address"
3. คัดลอกลิงก์มาวาง

---

## 📝 วิธีที่ 3: CSV with URLs (อัตโนมัติ)

ถ้าคุณมี CSV ที่มีคอลัมน์ `imageUrls` อยู่แล้ว:

```csv
รหัสสินค้า,ชื่อสินค้า,imageUrls
2594805678,น้ำยาทำความสะอาดรองเท้า,https://down-th.img.susercontent.com/file/xxx|https://down-th.img.susercontent.com/file/yyy
54103031406,Adidas Jacket,https://down-th.img.susercontent.com/file/zzz
```

รัน script แบบอัตโนมัติ:

```bash
node scripts/download-images-from-urls.js input-with-urls.csv output.csv
```

Script จะ:
- อ่านลิงก์รูปจากคอลัมน์ `imageUrls`
- ดาวน์โหลดรูปมาเก็บใน `/public/products/{productId}/`
- สร้าง CSV ใหม่พร้อมคอลัมน์ `downloadedImages` ที่มี path ไปยังรูปภาพ

---

## 📂 โครงสร้างไฟล์

หลังจากดาวน์โหลด รูปภาพจะถูกเก็บที่:

```
public/
  products/
    2594805678/
      image-1.jpg
      image-2.jpg
      image-3.jpg
    54103031406/
      image-1.jpg
      video-1.mp4
```

---

## 🎯 Workflow แนะนำ

### สำหรับสินค้าน้อย (< 10 ชิ้น)

```bash
# 1. เปิด dev server
npm run dev

# 2. เปิดเครื่องมือ
open http://localhost:3000/shopee-image-extractor.html

# 3. ใช้ Bookmarklet ดึงรูปจากแต่ละสินค้า

# 4. คัดลอกลิงก์ไปใส่ใน CSV

# 5. ดาวน์โหลดรูปภาพ
node scripts/download-images-from-urls.js products.csv products-with-images.csv
```

### สำหรับสินค้าเยอะ (> 10 ชิ้น)

```bash
# 1. ใช้ Interactive Script
node scripts/download-images-from-urls.js products.csv output.csv --interactive

# 2. เปิดหน้า Shopee แต่ละสินค้าใน browser
# 3. คลิกขวาที่รูป → Copy Image Address
# 4. วางลิงก์ใน Terminal
# 5. พิมพ์ "done" เมื่อเสร็จ
```

---

## 🔧 เครื่องมือที่สร้างไว้

### 1. **shopee-image-extractor.html**
หน้าเว็บที่มี:
- Bookmarklet สำหรับดึงรูปจาก Shopee
- UI สำหรับ preview รูปภาพ
- ปุ่มดาวน์โหลดรูปแต่ละรูป
- ปุ่มคัดลอกลิงก์ทั้งหมด

### 2. **download-images-from-urls.js**
Script Node.js ที่:
- อ่าน CSV
- ดาวน์โหลดรูปจากลิงก์
- เก็บรูปใน `/public/products/`
- สร้าง CSV ใหม่พร้อม path รูปภาพ

---

## ❓ FAQ

### Q: ทำไม Shopee API ใช้ไม่ได้?
A: Shopee API สำหรับ affiliate ต้องสมัครและมี API key ซึ่งไม่เปิดให้ทุกคน และ public API ก็ต้องการ authentication

### Q: Bookmarklet ทำงานได้กับทุก browser หรือไม่?
A: ใช้ได้กับ Chrome, Firefox, Safari, Edge ทุกเวอร์ชันที่รองรับ ES6

### Q: รูปที่ดาวน์โหลดมามีขนาดเท่าไหร่?
A: ขนาดเต็มจากที่ Shopee จัดเก็บ (ปกติ 800x800 ขึ้นไป)

### Q: ดาวน์โหลดวิดีโอได้ไหม?
A: Bookmarklet จะดึงลิงก์วิดีโอได้ แต่อาจต้องดาวน์โหลดด้วยตนเอง

### Q: ลิงก์รูปจาก Shopee หมดอายุไหม?
A: ลิงก์รูปของ Shopee มักไม่หมดอายุ แต่แนะนำให้ดาวน์โหลดมาเก็บไว้เอง

---

## 🎓 Tips

1. **ใช้ Bookmarklet กับหน้าสินค้า** - อย่าใช้กับหน้าอื่น (หน้าแรก, category) จะไม่ได้รูปที่ต้องการ

2. **รอหน้าเว็บโหลดเสร็จ** - ก่อนกด Bookmarklet ให้รอรูปภาพทั้งหมดโหลดเสร็จ

3. **เปิด DevTools ดูรูป** - กด F12 → Network → Img เพื่อดูลิงก์รูปทั้งหมด

4. **ใช้ susercontent.com** - ลิงก์รูปที่ดีที่สุดจะมี `down-th.img.susercontent.com` หรือ `cf.shopee.co.th`

5. **เลี่ยง thumbnail** - ลิงก์ที่มี `_tn` ใน URL คือรูปย่อ ไม่ควรใช้

---

## 🚀 Next Steps

หลังจากดาวน์โหลดรูปเรียบร้อยแล้ว:

1. **Import CSV เข้าระบบ** ผ่านหน้า Admin
2. **หรือใช้ Prisma seed script** ถ้ามีข้อมูลเยอะ
3. รูปภาพจะแสดงบนเว็บไซต์อัตโนมัติ

---

## 📞 ต้องการความช่วยเหลือ?

ถ้ามีปัญหาหรือต้องการฟีเจอร์เพิ่ม:
1. เปิด Issue บน GitHub
2. หรือติดต่อ Developer

---

**สร้างเมื่อ:** 2026-01-16
**เวอร์ชัน:** 1.0
**ใช้สำหรับ:** AffiliatePremium - Shopee Affiliate Website
