# 🚀 Quick Start: ดึงรูปจาก Shopee

วิธีเร็วที่สุดในการดึงรูปภาพจากสินค้า Shopee มาใส่ใน CSV

---

## วิธีที่ง่ายที่สุด (แนะนำ)

### 1. ติดตั้ง Playwright (ถ้ายังไม่มี)

```bash
npm install --save-dev playwright
npx playwright install chromium
```

### 2. เตรียม CSV ที่มีลิงก์สินค้า

สร้างไฟล์ `products.csv`:

```csv
รหัสสินค้า,ชื่อสินค้า,ลิงก์สินค้า
2594805678,น้ำยาทำความสะอาดรองเท้า,https://shopee.co.th/product/28794063/2594805678
54103031406,Adidas Jacket,https://shopee.co.th/product/1670209903/54103031406
```

### 3. รัน Script

```bash
# ทดสอบก่อน (3 สินค้าแรก)
node scripts/fetch-shopee-with-playwright.js products.csv output.csv --limit=3

# รันเต็มรูปแบบ (headless mode - เร็วกว่า)
node scripts/fetch-shopee-with-playwright.js products.csv output.csv --headless
```

### 4. เสร็จแล้ว!

รูปภาพจะถูกดาวน์โหลดไปที่:
```
public/products/2594805678/image-1.jpg
public/products/2594805678/image-2.jpg
public/products/54103031406/image-1.jpg
...
```

ไฟล์ `output.csv` จะมีคอลัมน์เพิ่ม:
- `imageUrls` - ลิงก์รูปต้นฉบับ
- `downloadedImages` - path รูปที่ดาวน์โหลด
- `imagesCount` - จำนวนรูป

---

## Options ที่มีประโยชน์

```bash
# แสดง browser (ดูว่ามันทำอะไร)
node scripts/fetch-shopee-with-playwright.js products.csv output.csv

# Headless mode (ไม่แสดง browser, เร็วกว่า)
node scripts/fetch-shopee-with-playwright.js products.csv output.csv --headless

# จำกัดจำนวนสินค้า
node scripts/fetch-shopee-with-playwright.js products.csv output.csv --limit=5

# หน่วงเวลาระหว่างสินค้า (ป้องกัน rate limit)
node scripts/fetch-shopee-with-playwright.js products.csv output.csv --delay=3000

# ดึงแค่ URL ไม่ดาวน์โหลด
node scripts/fetch-shopee-with-playwright.js products.csv output.csv --skip-download
```

---

## ตัวอย่างการใช้งาน

### สถานการณ์ 1: มี CSV จาก Shopee Affiliate

```bash
# 1. ดาวน์โหลด CSV จาก Shopee Affiliate Dashboard
# 2. รัน script
node scripts/fetch-shopee-with-playwright.js shopee-export.csv products-with-images.csv --headless

# เสร็จ! มีรูปครบแล้ว
```

### สถานการณ์ 2: ทดสอบก่อนรันเต็ม

```bash
# ทดสอบ 3 สินค้าแรก
node scripts/fetch-shopee-with-playwright.js products.csv test.csv --limit=3

# ดูผลลัพธ์ OK แล้ว รันเต็ม
node scripts/fetch-shopee-with-playwright.js products.csv final.csv --headless
```

### สถานการณ์ 3: Shopee บล็อก (rate limit)

```bash
# เพิ่มเวลาหน่วงระหว่างสินค้า
node scripts/fetch-shopee-with-playwright.js products.csv output.csv --delay=5000 --headless
```

---

## วิธีอื่นๆ (ถ้าไม่ต้องการใช้ Playwright)

### วิธีที่ 2: Bookmarklet (Manual แต่ไม่ต้องติดตั้ง)

1. เปิด `http://localhost:3000/shopee-image-extractor.html`
2. ลาก Bookmarklet ไปวาง Bookmark Bar
3. เปิดหน้าสินค้า Shopee → กด Bookmarklet
4. คัดลอกลิงก์รูปมาใส่ใน CSV

[ดูคู่มือเต็ม →](SHOPEE_IMAGE_GUIDE.md#-วิธีที่-2-bookmarklet--manual)

### วิธีที่ 3: Interactive Script

```bash
node scripts/download-images-from-urls.js products.csv output.csv --interactive
```

Script จะถามลิงก์รูปสำหรับแต่ละสินค้า

[ดูคู่มือเต็ม →](SHOPEE_IMAGE_GUIDE.md#️-วิธีที่-3-interactive-script-กึ่งอัตโนมัติ)

---

## FAQ

**Q: ทำไมดึงได้แค่ 1-2 รูป?**
A: Shopee บางครั้งโหลดรูปเพิ่มเมื่อ scroll ลอง scroll ดูรูปทั้งหมดก่อน หรือเพิ่ม `--delay` ให้มากขึ้น

**Q: Shopee บล็อกการเข้าถึงไหม?**
A: ใช้ `--delay=5000` เพื่อหน่วงเวลา หรือรันทีละน้อยๆ ด้วย `--limit`

**Q: สามารถดึงวิดีโอได้ไหม?**
A: ได้! Script จะดึงวิดีโอที่เจอในหน้าสินค้าด้วย

**Q: ต้องติดตั้ง Playwright ทุกครั้งหรือไม่?**
A: ไม่ ติดตั้งครั้งแรกครั้งเดียว

**Q: รูปที่ดาวน์โหลดมามีคุณภาพดีไหม?**
A: ได้รูปต้นฉบับจาก Shopee (ปกติ 800x800 หรือสูงกว่า)

---

## ปัญหาที่พบบ่อย

### Error: browser not installed
```bash
npx playwright install chromium
```

### Permission denied
```bash
chmod +x scripts/fetch-shopee-with-playwright.js
```

### No images found
- รอให้หน้าเว็บโหลดเสร็จ (เพิ่ม `--delay`)
- ตรวจสอบว่าลิงก์สินค้าถูกต้อง
- ลองรันแบบไม่ใช้ `--headless` เพื่อดูว่าเกิดอะไรขึ้น

---

## สรุป

```bash
# คำสั่งเดียวจบ (แนะนำ)
node scripts/fetch-shopee-with-playwright.js products.csv output.csv --headless
```

ดู[คู่มือฉบับสมบูรณ์](SHOPEE_IMAGE_GUIDE.md) สำหรับรายละเอียดเพิ่มเติม

---

**เวอร์ชัน:** 1.0
**อัพเดท:** 2026-01-16
