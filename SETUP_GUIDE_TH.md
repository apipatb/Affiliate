# 📚 วิธีติดตั้งเว็บไซต์ Affiliate

> คู่มือนี้สำหรับผู้เริ่มต้น ทำตามทีละขั้นตอนง่ายๆ

---

## 🌟 ต้องการผู้เชี่ยวชาญช่วยเหลือ?

> ถ้าต้องการผู้เชี่ยวชาญคอยช่วยเหลือจนสร้างเสร็จ, Template สำเร็จรูป, หรือเรียนรู้ n8n AI Automation
>
> **เข้าร่วม BoomBigNose+ ได้เลย!**
>
> 🔗 **https://www.skool.com/boombignose-8034/about**

---

## 📦 สิ่งที่ต้องมีก่อนเริ่ม

### ติดตั้ง Node.js (สำคัญมาก!)

Node.js คือโปรแกรมที่ใช้รันเว็บไซต์นี้ ต้องติดตั้งก่อนเริ่มใช้งาน

**วิธีติดตั้ง:**

1. ไปที่เว็บไซต์: **https://nodejs.org/**
2. คลิกปุ่มดาวน์โหลด **LTS** (ปุ่มสีเขียว)
3. เปิดไฟล์ที่โหลดมา แล้วติดตั้งตามขั้นตอน (กด Next ไปเรื่อยๆ)
4. รีสตาร์ทคอมพิวเตอร์

**ตรวจสอบว่าติดตั้งสำเร็จ:**

เปิด Terminal (หรือ Command Prompt) แล้วพิมพ์:
```bash
node --version
```

ถ้าขึ้นเลขเวอร์ชัน เช่น `v20.10.0` แสดงว่าติดตั้งสำเร็จ ✅

---

## 🚀 ขั้นตอนการติดตั้ง

### ขั้นตอนที่ 1: แตกไฟล์ ZIP

แตกไฟล์ ZIP ที่ได้รับมา ไปไว้ในโฟลเดอร์ที่ต้องการ

### ขั้นตอนที่ 2: เปิด Terminal

- **Windows**: คลิกขวาในโฟลเดอร์ → "Open in Terminal"
- **Mac**: คลิกขวา → "New Terminal at Folder"

หรือเปิด Terminal แล้วพิมพ์:
```bash
cd /path/to/your/folder
```

### ขั้นตอนที่ 3: ติดตั้ง Packages

```bash
npm install
```
รอจนเสร็จ (อาจใช้เวลา 1-3 นาที)

### ขั้นตอนที่ 4: สร้างฐานข้อมูล

```bash
npx prisma generate
npm run db:push
npm run db:seed
```

### ขั้นตอนที่ 5: รันเว็บไซต์

```bash
npm run dev
```

### ขั้นตอนที่ 6: เปิดเว็บไซต์

เปิด Browser แล้วไปที่: **http://localhost:3000**

---

## 🔐 หน้า Admin และการเข้าสู่ระบบ

### เข้าหน้า Admin

เปิด Browser ไปที่: **http://localhost:3000/admin/login**

### ข้อมูล Login เริ่มต้น (Default)

| ข้อมูล | ค่าเริ่มต้น |
|--------|-------------|
| **Email** | `admin@example.com` |
| **Password** | `admin123` |

> ⚠️ **สำคัญ:** ค่าเหล่านี้คือค่าเริ่มต้น ถ้าต้องการเปลี่ยนให้ดูหัวข้อถัดไป

---

## ⚙️ เปลี่ยน Email และ Password ของ Admin

ถ้าต้องการใช้ Email หรือ Password อื่น ทำตามขั้นตอนนี้:

### ขั้นตอนที่ 1: สร้างไฟล์ `.env`

สร้างไฟล์ชื่อ `.env` ในโฟลเดอร์หลักของโปรเจกต์ (โฟลเดอร์เดียวกับ package.json)

### ขั้นตอนที่ 2: ใส่ข้อมูลในไฟล์ `.env`

```
ADMIN_EMAIL=your-email@example.com
ADMIN_PASSWORD=your-new-password
```

**ตัวอย่าง:**
```
ADMIN_EMAIL=myemail@gmail.com
ADMIN_PASSWORD=MySecurePassword123
```

### ขั้นตอนที่ 3: รันคำสั่งอัพเดท Admin

```bash
npm run db:seed:admin
```

### ขั้นตอนที่ 4: Login ด้วยข้อมูลใหม่

ไปที่ **http://localhost:3000/admin/login** แล้วใช้ Email และ Password ที่ตั้งใหม่

> 💡 **หมายเหตุ:** ถ้าไม่สร้างไฟล์ `.env` ระบบจะใช้ค่าเริ่มต้น (`admin@example.com` / `admin123`)

---

## 📝 คำสั่งที่ใช้บ่อย

| สิ่งที่ต้องการทำ | คำสั่ง |
|-----------------|--------|
| รันเว็บไซต์ | `npm run dev` |
| หยุดเว็บไซต์ | กด `Ctrl + C` |
| ดูฐานข้อมูล | `npm run db:studio` |
| อัพเดท Admin | `npm run db:seed:admin` |

---

## ❓ ปัญหาที่พบบ่อย

### ขึ้น Error "node is not recognized"?
ยังไม่ได้ติดตั้ง Node.js หรือยังไม่ได้รีสตาร์ทคอม
→ กลับไปติดตั้ง Node.js ใหม่แล้วรีสตาร์ท

### เว็บไม่ขึ้น?

```bash
npm run db:push
npm run dev
```

### Login ไม่ได้?

ตรวจสอบว่าใช้ข้อมูลถูกต้อง:
- ถ้าไม่ได้สร้างไฟล์ `.env` ใช้: `admin@example.com` / `admin123`
- ถ้าสร้างไฟล์ `.env` แล้ว ใช้ค่าที่ตั้งไว้ในไฟล์นั้น

### ลืมรหัสผ่าน Admin?

แก้ไขไฟล์ `.env` ใส่รหัสผ่านใหม่ แล้วรัน:
```bash
npm run db:seed:admin
```

### Error อะไรก็ไม่รู้?

ลองรันใหม่ทั้งหมด:
```bash
npm install
npx prisma generate
npm run db:push
npm run db:seed
npm run dev
```

---

## 🎯 สรุปขั้นตอนรวดเร็ว

```bash
# 1. ติดตั้ง
npm install

# 2. สร้างฐานข้อมูล
npx prisma generate
npm run db:push
npm run db:seed

# 3. รัน
npm run dev

# 4. เปิด Browser
# หน้าเว็บ: http://localhost:3000
# หน้า Admin: http://localhost:3000/admin/login

# 5. Login Admin
# Email: admin@example.com
# Password: admin123
```

---

## 🌟 ต้องการความช่วยเหลือเพิ่มเติม?

> **เข้าร่วม BoomBigNose+ Community!**
> 
> 🔗 **https://www.skool.com/boombignose-8034/about**
>
> สิ่งที่คุณจะได้:
> - ✅ ผู้เชี่ยวชาญคอยช่วยเหลือจนสร้างเสร็จ
> - ✅ Template สำเร็จรูปสำหรับทุกโปรเจกต์
> - ✅ เรียนรู้ n8n AI Automation
> - ✅ VIP Support ตอบไวภายใน 24 ชม.

---

*สร้างด้วย ❤️ โดย BoomBigNose*

*ขอให้สนุกกับเว็บไซต์ Affiliate ของคุณ! 🎉*
